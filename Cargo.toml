[package]
name = "huttripper-shared"
version = "0.1.0"
edition = "2021"

[dependencies]
aws-config = {version = "1.1.4", features = ["behavior-version-latest"] }
aws-sdk-s3 = "1.14.0"
serde = { version = "1.0.196", features = ["derive"] }
